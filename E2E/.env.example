# Copy to .env and adjust if needed, then load it in your shell before running tests
# (or just set these as real environment variables). Never point E2E_DB_NAME at
# TruckCompanyDev or the production database.

E2E_PORT=5199
E2E_DB_SERVER=(localdb)\MSSQLLocalDB
E2E_DB_NAME=TruckCompanyE2E
E2E_ADMIN_EMAIL=e2e-admin@test.local
E2E_ADMIN_PASSWORD=E2eTestPass123!
